$("#modalLiveLabel").html("<%= action == 'new' ? I18n.t('.reports.report.edit') : I18n.t('.reports.report.edit') %>");
$(".modal-body").html("<%= j render 'form', report: @report %>");
$("#modalLive").modal("show");
