<% provide :title, @user.name %>
<div class="row">
  <% if @reports.any? %>
     <h3>(<%= @pagy.count %>)</h3>
      <%= link_to t(".create_report"), new_report_url, class: "btn btn-success btn-lg btn-block"%>
      <ol class="reports">
        <%= render @reports %>
      </ol>
    <%== pagy_bootstrap_nav(@pagy) %>
  <% end %>
</div>
